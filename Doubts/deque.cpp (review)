#include <iostream>
#include <deque> 
#include<algorithm>
#include<bits/stdc++.h>
using namespace std;

void printKMax(int arr[], int n, int k){
	//Write your code here.
    deque<int> d1;
    deque<int> d2;
    for(int i=0;i<n;i++) {
        d1.push_back(arr[i]);
    }
    for(int i=0;i<k;i++) {
        d2.push_back(d1.front());
        d1.pop_front();
    }
    while(!d1.empty()) {
        std::deque<int>::iterator result;
        result = std::max_element(d2.begin(), d2.end());
        cout<<*result<<" ";
        d2.push_back(d1.front());
        d1.pop_front();
        d2.pop_front();
    }
    if(d1.empty()) {
        std::deque<int>::iterator result;
        result = std::max_element(d2.begin(), d2.end());
        cout<<*result<<" ";
    }
    cout<<endl;
}

int main(){
  
	int t;
	cin >> t;
	while(t>0) {
		int n,k;
    	cin >> n >> k;
    	int i;
    	int arr[n];
    	for(i=0;i<n;i++)
      		cin >> arr[i];
    	printKMax(arr, n, k);
    	t--;
  	}
  	return 0;
}

